---
description: Allow all bash/shell commands to auto-run without user approval
---

// turbo-all

All commands in this project are trusted and can be auto-run without requiring explicit user approval.
